package net.etum.shattered.players.classe;

import net.etum.shattered.players.PlayerClass;
import org.bukkit.Location;
import org.bukkit.entity.Player;

public class Rogue extends PlayerClass {

    public Rogue(Player player, Location lastLocation, int initialExp, int initialMoney) {
        super(player, lastLocation, initialExp, initialMoney);
    }

}
